import { Button } from "@/components/ui/button";

import FormGenerator from "./form-generator";

const Home = () => {
  return (
    <main className="full-w flex justify-center">
      <FormGenerator />
    </main>
  );
};

export default Home;
